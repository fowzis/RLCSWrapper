<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>RLCSWrapper.Test</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <!-- Copy RL libraries to RLlib folder after build -->
  <!-- Disabled: Requires SolutionDir which may not be available when building individual projects -->
  <!-- <Target Name="CopyRLLibraries" AfterTargets="Build">
    <PropertyGroup>
      <RLlibPath>$(SolutionDir)RLlib</RLlibPath>
      <WindowsLibPath>$(RLlibPath)\Windows</WindowsLibPath>
      <LinuxLibPath>$(RLlibPath)\Linux</LinuxLibPath>
      <MacOSLibPath>$(RLlibPath)\macOS</MacOSLibPath>
    </PropertyGroup>
    
    <MakeDir Directories="$(WindowsLibPath);$(LinuxLibPath);$(MacOSLibPath)" Condition="!Exists('$(WindowsLibPath)')" />
    
    <Message Text="RL libraries should be copied to RLlib\Windows, RLlib\Linux, or RLlib\macOS folders" Importance="high" />
  </Target> -->

</Project>
